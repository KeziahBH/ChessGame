services:

  frontend:
    build: ./frontend
    container_name: shatranj-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: shatranj-backend
    ports:
      - "8080:8080"
